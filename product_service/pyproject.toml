[project]
name = "product-service"
version = "0.0.1"
description = "service that helps to accept requests to purchase goods by checking their availability in an external service"
authors = [
    {name = "nailed1",email = "max20070608@gmail.com"}
]
license = "MIT"
requires-python = "^3.12"
dependencies = [
    "fastapi (>=0.136.1,<0.137.0)",
    "uvicorn (>=0.46.0,<0.47.0)",
    "httpx (>=0.28.1,<0.29.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
